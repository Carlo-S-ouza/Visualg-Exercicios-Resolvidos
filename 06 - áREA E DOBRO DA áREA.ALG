Algoritmo "Area e dobro da área"

Var
   num1, r1, r2: real

Inicio
   escreval("Qual o tamanho, em cm, do lado do quadrado?")
   leia (num1)

   r1 <- num1*num1
   r2 <- r1*2
   limpatela
   escreval("A área do quadrado é de ", r1, "cm²")
   escreval("O dobro dessa área é de ", r2, "cm²")

Fimalgoritmo