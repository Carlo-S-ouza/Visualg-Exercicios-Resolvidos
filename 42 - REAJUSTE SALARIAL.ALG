Algoritmo "42 - Reajuste Salarial"

Var
salario, resultado: real

Inicio
    escreval("Digite o salário atual:")
    leia (salario)
    resultado <- (salario * 1.15)
    escreval("O novo salário será de R$", resultado)

Fimalgoritmo
